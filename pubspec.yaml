name: untitled
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.0
  google_mlkit_text_recognition: ^0.14.0
  googleapis: ^12.0.0
  googleapis_auth: ^1.3.0
  http: ^1.2.0
  url_launcher: ^6.1.7
  provider: ^6.0.5  # Dependência adicionada

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/credentials.json
